# 统一换行符为 LF（与各 .editorconfig 一致）。
# text=auto 让 Git 按内容自动识别文本/二进制，文本统一规范为 LF。
* text=auto eol=lf

# 显式二进制，避免被 text=auto 误判
*.bin binary
*.hex binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
