# 硬件在环测试（HIL）

真机测试占位。落地 M4/M5 标定时在此添加：

- 标准电阻（100Ω / 1kΩ / 10kΩ）各测 10 次，误差 ≤0.5%（FR-1 验收）
- 采样率三档实际值偏差 <1%（FR-2 验收）
- 连续 1h 丢包 / 延迟统计（FR-3 验收）

## 运行

```bash
pio test -d firmware -e esp32dev
```

需接线：ADS1220 SPI + DRDY 中断 + 标准电阻。
